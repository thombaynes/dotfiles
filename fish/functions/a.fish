#!/usr/local/bin/fish
# A script written by someone else at UT that only works in ZSH.
# I have it saved as a ZSH alias. See the script in the root directory /ut_aws.sh
function a
   zsh -c '
   source ~/.zshrc
   a'
end
